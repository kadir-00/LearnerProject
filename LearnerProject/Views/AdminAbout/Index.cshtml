@model List<LearnerProject.Models.Entitities.About>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h3>Hakkimda</h3>
@{
    int count = 0;
}

<table class="table table-bordered table-hover">
    <tr>
        <th>#</th>
        <th>Baslik</th>
        <th>Aciklama</th>
        <th>Resim</th>
        <th>Video Linki</th>
        <th>Item1</th>
        <th>Item2</th>
        <th>Item3</th>

    </tr>

    @foreach (var item in Model)
    {
        count++;
        <tr>
            <th>@count</th>
            <td>@item.Title</td>
            <td>@item.Description</td>
            <td>@item.ImageUrl</td>
            <td>@item.VideoUrl</td>
            <td>@item.Item1</td>
            <td>@item.Item2</td>
            <td>@item.Item3</td>

            <td>
                @*// i icon eklemek icin*@
                <a href="/AdminAbout/DeleteAbout/@item.AboutId" class="btn btn-outline-danger btn-sm m-1"> <i
                        class="fa fa-trash"></i> Sil</a>
                <a href="/AdminAbout/UpdateAbout/@item.AboutId" class="btn btn-outline-info btn-sm m-1"> <i
                        class="fa fa-pen"></i> Güncelle</a>
            </td>
        </tr>
    }

</table>
<a href="/AdminAbout/AddAbout" class="btn btn-outline-success"><i class="fa fa-plus m-2"></i> Ekle</a>
